import {createSlice} from '@reduxjs/toolkit';

export const sewistReducer = createSlice({
   name: 'counter',
   initialState: {},
   reducers: {},
});

export const {} = sewistReducer.actions;

export default sewistReducer.reducer;
