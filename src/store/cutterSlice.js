import {createSlice} from '@reduxjs/toolkit';

export const cutterReducer = createSlice({
   name: 'counter',
   initialState: {},
   reducers: {},
});

export const {} = cutterReducer.actions;

export default cutterReducer.reducer;
