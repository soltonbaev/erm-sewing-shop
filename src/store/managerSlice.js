import {createSlice} from '@reduxjs/toolkit';

export const managerReducer = createSlice({
   name: 'counter',
   initialState: {},
   reducers: {},
});

export const {} = managerReducer.actions;

export default managerReducer.reducer;
